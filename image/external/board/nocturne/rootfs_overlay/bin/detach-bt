#!/bin/sh

BTATTACH_PID=/run/btattach.pid

echo "|-----Bluetooth STOPPING-----|"

if [ -f ${BTATTACH_PID} ]; then
  start-stop-daemon --stop --quiet --pidfile ${BTATTACH_PID} --retry 5
  rm -f ${BTATTACH_PID}
fi
