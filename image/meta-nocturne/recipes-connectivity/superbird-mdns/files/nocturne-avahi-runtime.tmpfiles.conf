# Avahi reads service files from /etc/avahi/services and /run/avahi/services.
# Our rootfs is read-only at /etc, so the nocturne service is rendered into
# /run by superbird-init.service at boot.
d /run/avahi          0755 avahi avahi - -
d /run/avahi/services 0755 avahi avahi - -
