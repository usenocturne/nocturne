[Unit]
Wants=nocturned.service
After=nocturned.service

[Service]
ExecStartPre=/bin/sh -c 'for i in $(seq 1 60); do /usr/bin/wget -q -T 1 -O /dev/null http://127.0.0.1:8080/ && exit 0; sleep 0.5; done; exit 1'
