services:
  nix:
    build:
      context: ./docker/
      dockerfile: ./Dockerfile
    volumes:
      - ./:/workdir
      - nix-store:/nix
      - nix-root:/root
    stdin_open: true
    tty: true

volumes:
  nix-store:
  nix-root:
