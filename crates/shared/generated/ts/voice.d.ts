// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type VoiceJsonValue = string | number | boolean | null | VoiceJsonValue[] | { [key: string]: VoiceJsonValue };

/**
 * Event payload for `ai.response`.
 * AI assistant response event.
 * Inventory: `EVENT_INVENTORY` entry `ai.response` payload.
 */
export interface AiResponseEvent {
  /**
   * Assistant response text. Inventory field `message` emits as `message`.
   */
  message?: string;
  /**
   * Legacy response text alias. Inventory field `text` emits as `text`.
   */
  text?: string;
  /**
   * Whether this response chunk is final. Inventory field `is_final` emits as `isFinal`.
   */
  isFinal?: boolean;
  /**
   * Voice session identifier used to reject stale turn events. Inventory field `session_id` emits as `sessionId`.
   */
  sessionId?: string;
}

/**
 * Event envelope for `ai.response` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `ai.response`.
 */
export interface AiResponseEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "ai.response";
  /**
   * Payload carried by this inventory variant.
   */
  data: AiResponseEvent;
}

/**
 * Event payload for `ai.state`.
 * AI assistant state event.
 * Inventory: `EVENT_INVENTORY` entry `ai.state` payload.
 */
export interface AiStateEvent {
  /**
   * Assistant state. Inventory field `state` emits as `state`.
   */
  state: string;
  /**
   * Optional status text. Inventory field `message` emits as `message`.
   */
  message?: string;
  /**
   * Voice session identifier used to reject stale turn events. Inventory field `session_id` emits as `sessionId`.
   */
  sessionId?: string;
}

/**
 * Event envelope for `ai.state` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `ai.state`.
 */
export interface AiStateEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "ai.state";
  /**
   * Payload carried by this inventory variant.
   */
  data: AiStateEvent;
}

/**
 * Event payload for `ai.tool_executed`.
 * AI tool execution event.
 * Inventory: `EVENT_INVENTORY` entry `ai.tool_executed` payload.
 */
export interface AiToolExecutedEvent {
  /**
   * Executed tool name. Inventory field `tool_name` emits as `toolName`.
   */
  toolName?: string;
  /**
   * Legacy executed tool name alias. Inventory field `tool` emits as `tool`.
   */
  tool?: string;
  /**
   * LLM tool-call id. Inventory field `call_id` emits as `callId`.
   */
  callId?: string;
  /**
   * Tool execution status. Inventory field `status` emits as `status`.
   */
  status?: string;
  /**
   * Tool arguments decoded from the LLM call. Inventory field `tool_arguments` emits as `toolArguments`.
   */
  toolArguments?: VoiceJsonValue;
  /**
   * Tool-specific result. Inventory field `result` emits as `result`.
   */
  result?: VoiceJsonValue;
  /**
   * Tool error text. Inventory field `error` emits as `error`.
   */
  error?: string;
  /**
   * Voice session identifier used to reject stale turn events. Inventory field `session_id` emits as `sessionId`.
   */
  sessionId?: string;
}

/**
 * Event envelope for `ai.tool_executed` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `ai.tool_executed`.
 */
export interface AiToolExecutedEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "ai.tool_executed";
  /**
   * Payload carried by this inventory variant.
   */
  data: AiToolExecutedEvent;
}

/**
 * Request envelope for `tts.speak` in the `voice` method union.
 * Inventory: `METHOD_INVENTORY` entry `tts.speak`.
 */
export interface TtsSpeakMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "tts.speak";
  /**
   * Payload carried by this inventory variant.
   */
  data: TtsSpeakRequest;
}

/**
 * Response envelope for `tts.speak` in the `voice` method response union.
 * Inventory: `METHOD_INVENTORY` entry `tts.speak`.
 */
export interface TtsSpeakMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "tts.speak";
  /**
   * Payload carried by this inventory variant.
   */
  data: TtsSpeakResponse;
}

/**
 * Request payload for `tts.speak`.
 * Text-to-speech request.
 * Inventory: `METHOD_INVENTORY` entry `tts.speak` request.
 */
export interface TtsSpeakRequest {
  /**
   * Text to speak. Inventory field `text` emits as `text`.
   */
  text: string;
  /**
   * Optional voice identifier. Inventory field `voice` emits as `voice`.
   */
  voice?: string;
}

/**
 * Response payload for `tts.speak`.
 * Opaque JSON payload owned by the companion app or legacy daemon code.
 * Inventory: `METHOD_INVENTORY` entry `tts.speak` response.
 */
export interface TtsSpeakResponse {
}

/**
 * Request envelope for `tts.stop` in the `voice` method union.
 * Inventory: `METHOD_INVENTORY` entry `tts.stop`.
 */
export interface TtsStopMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "tts.stop";
  /**
   * Payload carried by this inventory variant.
   */
  data: TtsStopRequest;
}

/**
 * Response envelope for `tts.stop` in the `voice` method response union.
 * Inventory: `METHOD_INVENTORY` entry `tts.stop`.
 */
export interface TtsStopMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "tts.stop";
  /**
   * Payload carried by this inventory variant.
   */
  data: TtsStopResponse;
}

/**
 * Request payload for `tts.stop`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `tts.stop` request.
 */
export interface TtsStopRequest {
}

/**
 * Response payload for `tts.stop`.
 * Opaque JSON payload owned by the companion app or legacy daemon code.
 * Inventory: `METHOD_INVENTORY` entry `tts.stop` response.
 */
export interface TtsStopResponse {
}

/**
 * Request envelope for `voice.cancel` in the `voice` method union.
 * Inventory: `METHOD_INVENTORY` entry `voice.cancel`.
 */
export interface VoiceCancelMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "voice.cancel";
  /**
   * Payload carried by this inventory variant.
   */
  data: VoiceCancelRequest;
}

/**
 * Response envelope for `voice.cancel` in the `voice` method response union.
 * Inventory: `METHOD_INVENTORY` entry `voice.cancel`.
 */
export interface VoiceCancelMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "voice.cancel";
  /**
   * Payload carried by this inventory variant.
   */
  data: VoiceCancelResponse;
}

/**
 * Request payload for `voice.cancel`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `voice.cancel` request.
 */
export interface VoiceCancelRequest {
}

/**
 * Response payload for `voice.cancel`.
 * Opaque JSON payload owned by the companion app or legacy daemon code.
 * Inventory: `METHOD_INVENTORY` entry `voice.cancel` response.
 */
export interface VoiceCancelResponse {
}

/**
 * Discriminated event union for the `voice` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type VoiceEvent =
  | VoiceWakewordEventMessage
  | VoiceWakewordStateEventMessage
  | VoiceTranscriptionEventMessage
  | AiStateEventMessage
  | AiResponseEventMessage
  | AiToolExecutedEventMessage
;

/**
 * Discriminated method-request union for the `voice` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type VoiceMethod =
  | WakewordPauseMethodMessage
  | WakewordResumeMethodMessage
  | TtsSpeakMethodMessage
  | TtsStopMethodMessage
  | VoiceCancelMethodMessage
;

/**
 * Discriminated method-response union for the `voice` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type VoiceMethodResponse =
  | WakewordPauseMethodResponseMessage
  | WakewordResumeMethodResponseMessage
  | TtsSpeakMethodResponseMessage
  | TtsStopMethodResponseMessage
  | VoiceCancelMethodResponseMessage
;

/**
 * Event payload for `voice.transcription`.
 * Voice transcription event.
 * Inventory: `EVENT_INVENTORY` entry `voice.transcription` payload.
 */
export interface VoiceTranscriptionEvent {
  /**
   * Transcribed text. Inventory field `transcript` emits as `transcript`.
   */
  transcript: string;
  /**
   * Whether this transcript is final. Inventory field `is_final` emits as `isFinal`.
   */
  isFinal: boolean;
  /**
   * Voice session identifier used to reject stale turn events. Inventory field `session_id` emits as `sessionId`.
   */
  sessionId?: string;
}

/**
 * Event envelope for `voice.transcription` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `voice.transcription`.
 */
export interface VoiceTranscriptionEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "voice.transcription";
  /**
   * Payload carried by this inventory variant.
   */
  data: VoiceTranscriptionEvent;
}

/**
 * Event payload for `voice.wakeword`.
 * Wakeword detection event.
 * Inventory: `EVENT_INVENTORY` entry `voice.wakeword` payload.
 */
export interface VoiceWakewordEvent {
  /**
   * Detected wake word keyword. Inventory field `keyword` emits as `keyword`.
   */
  keyword: string;
  /**
   * Classifier confidence. Inventory field `confidence` emits as `confidence`.
   */
  confidence: number;
}

/**
 * Event envelope for `voice.wakeword` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `voice.wakeword`.
 */
export interface VoiceWakewordEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "voice.wakeword";
  /**
   * Payload carried by this inventory variant.
   */
  data: VoiceWakewordEvent;
}

/**
 * Event payload for `voice.wakeword.state`.
 * Wakeword muted state.
 * Inventory: `EVENT_INVENTORY` entry `voice.wakeword.state` payload.
 */
export interface VoiceWakewordStateEvent {
  /**
   * Whether wakeword detection is muted. Inventory field `muted` emits as `muted`.
   */
  muted: boolean;
}

/**
 * Event envelope for `voice.wakeword.state` in the `voice` event union.
 * Inventory: `EVENT_INVENTORY` entry `voice.wakeword.state`.
 */
export interface VoiceWakewordStateEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "voice.wakeword.state";
  /**
   * Payload carried by this inventory variant.
   */
  data: VoiceWakewordStateEvent;
}

/**
 * Request envelope for `wakeword.pause` in the `voice` method union.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.pause`.
 */
export interface WakewordPauseMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "wakeword.pause";
  /**
   * Payload carried by this inventory variant.
   */
  data: WakewordPauseRequest;
}

/**
 * Response envelope for `wakeword.pause` in the `voice` method response union.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.pause`.
 */
export interface WakewordPauseMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "wakeword.pause";
  /**
   * Payload carried by this inventory variant.
   */
  data: WakewordPauseResponse;
}

/**
 * Request payload for `wakeword.pause`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.pause` request.
 */
export interface WakewordPauseRequest {
}

/**
 * Response payload for `wakeword.pause`.
 * Wakeword pause acknowledgement.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.pause` response.
 */
export interface WakewordPauseResponse {
  /**
   * paused. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `wakeword.resume` in the `voice` method union.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.resume`.
 */
export interface WakewordResumeMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "wakeword.resume";
  /**
   * Payload carried by this inventory variant.
   */
  data: WakewordResumeRequest;
}

/**
 * Response envelope for `wakeword.resume` in the `voice` method response union.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.resume`.
 */
export interface WakewordResumeMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "wakeword.resume";
  /**
   * Payload carried by this inventory variant.
   */
  data: WakewordResumeResponse;
}

/**
 * Request payload for `wakeword.resume`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.resume` request.
 */
export interface WakewordResumeRequest {
}

/**
 * Response payload for `wakeword.resume`.
 * Wakeword resume acknowledgement.
 * Inventory: `METHOD_INVENTORY` entry `wakeword.resume` response.
 */
export interface WakewordResumeResponse {
  /**
   * resumed. Inventory field `status` emits as `status`.
   */
  status: string;
}

