// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type AudioJsonValue = string | number | boolean | null | AudioJsonValue[] | { [key: string]: AudioJsonValue };

/**
 * Discriminated event union for the `audio` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type AudioEvent =
  | AudioLevelEventMessage
  | WindLevelEventMessage
;

/**
 * Event payload for `audio.level`.
 * Mic level event.
 * Inventory: `EVENT_INVENTORY` entry `audio.level` payload.
 */
export interface AudioLevelEvent {
  /**
   * Normalized microphone level 0..1. Inventory field `level` emits as `level`.
   */
  level: number;
}

/**
 * Event envelope for `audio.level` in the `audio` event union.
 * Inventory: `EVENT_INVENTORY` entry `audio.level`.
 */
export interface AudioLevelEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "audio.level";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioLevelEvent;
}

/**
 * Discriminated method-request union for the `audio` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type AudioMethod =
  | AudioRecordStartMethodMessage
  | AudioRecordStopMethodMessage
;

/**
 * Discriminated method-response union for the `audio` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type AudioMethodResponse =
  | AudioRecordStartMethodResponseMessage
  | AudioRecordStopMethodResponseMessage
;

/**
 * Request envelope for `audio.record.start` in the `audio` method union.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.start`.
 */
export interface AudioRecordStartMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "audio.record.start";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordStartRequest;
}

/**
 * Response envelope for `audio.record.start` in the `audio` method response union.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.start`.
 */
export interface AudioRecordStartMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "audio.record.start";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordStartResponse;
}

/**
 * Request payload for `audio.record.start`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.start` request.
 */
export interface AudioRecordStartRequest {
}

/**
 * Response payload for `audio.record.start`.
 * Recording start acknowledgement.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.start` response.
 */
export interface AudioRecordStartResponse {
  /**
   * recording. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `audio.record.stop` in the `audio` method union.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.stop`.
 */
export interface AudioRecordStopMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "audio.record.stop";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordStopRequest;
}

/**
 * Response envelope for `audio.record.stop` in the `audio` method response union.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.stop`.
 */
export interface AudioRecordStopMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "audio.record.stop";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordStopResponse;
}

/**
 * Request payload for `audio.record.stop`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.stop` request.
 */
export interface AudioRecordStopRequest {
}

/**
 * Response payload for `audio.record.stop`.
 * Recording stop acknowledgement.
 * Inventory: `METHOD_INVENTORY` entry `audio.record.stop` response.
 */
export interface AudioRecordStopResponse {
  /**
   * idle. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Event payload for `wind_level`.
 * Microphone wind interference level event.
 * Inventory: `EVENT_INVENTORY` entry `wind_level` payload.
 */
export interface WindLevelEvent {
  /**
   * Stock-compatible wind interference level from 0 to 4. Inventory field `level` emits as `level`.
   */
  level: number;
  /**
   * Smoothed wind interference score from 0 to 100. Inventory field `stat` emits as `stat`.
   */
  stat: number;
}

/**
 * Event envelope for `wind_level` in the `audio` event union.
 * Inventory: `EVENT_INVENTORY` entry `wind_level`.
 */
export interface WindLevelEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "wind_level";
  /**
   * Payload carried by this inventory variant.
   */
  data: WindLevelEvent;
}

