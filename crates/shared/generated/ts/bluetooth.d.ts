// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type BluetoothJsonValue = string | number | boolean | null | BluetoothJsonValue[] | { [key: string]: BluetoothJsonValue };

/**
 * Event payload for `bluetooth.agent`.
 * Bluetooth agent event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.agent` payload.
 */
export interface BluetoothAgentEvent {
  /**
   * Agent callback event name. Inventory field `event` emits as `event`.
   */
  event?: string;
  /**
   * BlueZ D-Bus device path. Inventory field `device` emits as `device`.
   */
  device?: string;
  /**
   * Bluetooth address. Inventory field `address` emits as `address`.
   */
  address?: string;
  /**
   * Device display name. Inventory field `name` emits as `name`.
   */
  name?: string;
  /**
   * PIN/passkey displayed to user. Inventory field `pin` emits as `pin`.
   */
  pin?: string;
  /**
   * PIN code for RequestPinCode. Inventory field `pincode` emits as `pincode`.
   */
  pincode?: string;
  /**
   * Legacy event type, e.g. bluetooth_pin. Inventory field `type` emits as `type`.
   */
  type?: string;
  /**
   * Numeric passkey. Inventory field `passkey` emits as `passkey`.
   */
  passkey?: number;
  /**
   * Digits entered. Inventory field `entered` emits as `entered`.
   */
  entered?: number;
  /**
   * Authorized service UUID. Inventory field `uuid` emits as `uuid`.
   */
  uuid?: string;
  /**
   * Auto-accept result. Inventory field `accepted` emits as `accepted`.
   */
  accepted?: boolean;
}

/**
 * Event envelope for `bluetooth.agent` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.agent`.
 */
export interface BluetoothAgentEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.agent";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothAgentEvent;
}

/**
 * Event payload for `bluetooth.connection`.
 * Bluetooth connection event.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.connection` payload.
 */
export interface BluetoothConnectionEvent {
  /**
   * connecting, connector_probe, connection_established, or connection_closed. Inventory field `event` emits as `event`.
   */
  event: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
  /**
   * rfcomm, iap2, generic, android, or auto. Inventory field `connection_type` emits as `connectionType`.
   */
  connectionType?: string;
  /**
   * Detected peer type. Inventory field `device_type` emits as `deviceType`.
   */
  deviceType?: string;
  /**
   * RFCOMM channel. Inventory field `channel` emits as `channel`.
   */
  channel?: number;
  /**
   * daemon or user. Inventory field `initiated_by` emits as `initiatedBy`.
   */
  initiatedBy?: string;
}

/**
 * Event envelope for `bluetooth.connection` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.connection`.
 */
export interface BluetoothConnectionEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.connection";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothConnectionEvent;
}

/**
 * Request envelope for `bluetooth.device.connect` in the `bluetooth` method union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.connect`.
 */
export interface BluetoothDeviceConnectMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.connect";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceConnectRequest;
}

/**
 * Response envelope for `bluetooth.device.connect` in the `bluetooth` method response union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.connect`.
 */
export interface BluetoothDeviceConnectMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.connect";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceConnectResponse;
}

/**
 * Request payload for `bluetooth.device.connect`.
 * Connect to a paired device.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.connect` request.
 */
export interface BluetoothDeviceConnectRequest {
  /**
   * BlueZ device MAC address. Inventory field `address` emits as `address`.
   */
  address: string;
  /**
   * Optional RFCOMM channel hint; 3 forces the macOS connector probe, otherwise the daemon auto-detects. Inventory field `channel` emits as `channel`.
   */
  channel?: number;
  /**
   * Optional peer type hint; computer/mac/macos/macos_connector force the macOS connector probe. Inventory field `device_type` emits as `deviceType`.
   */
  deviceType?: string;
}

/**
 * Response payload for `bluetooth.device.connect`.
 * Connection result.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.connect` response.
 */
export interface BluetoothDeviceConnectResponse {
  /**
   * connected, waiting_for_ios, waiting_for_macos_connector, or waiting_for_android. Inventory field `status` emits as `status`.
   */
  status: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
}

/**
 * Request envelope for `bluetooth.device.disconnect` in the `bluetooth` method union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.disconnect`.
 */
export interface BluetoothDeviceDisconnectMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.disconnect";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceDisconnectRequest;
}

/**
 * Response envelope for `bluetooth.device.disconnect` in the `bluetooth` method response union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.disconnect`.
 */
export interface BluetoothDeviceDisconnectMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.disconnect";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceDisconnectResponse;
}

/**
 * Request payload for `bluetooth.device.disconnect`.
 * Disconnect a device.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.disconnect` request.
 */
export interface BluetoothDeviceDisconnectRequest {
  /**
   * BlueZ device MAC address. Inventory field `address` emits as `address`.
   */
  address: string;
}

/**
 * Response payload for `bluetooth.device.disconnect`.
 * Disconnect result.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.disconnect` response.
 */
export interface BluetoothDeviceDisconnectResponse {
  /**
   * disconnected. Inventory field `status` emits as `status`.
   */
  status: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
}

/**
 * Event payload for `bluetooth.device`.
 * Bluetooth device event.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.device` payload.
 */
export interface BluetoothDeviceEvent {
  /**
   * connected, disconnected, removed, or unpaired. Inventory field `event` emits as `event`.
   */
  event: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
}

/**
 * Event envelope for `bluetooth.device` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.device`.
 */
export interface BluetoothDeviceEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.device";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceEvent;
}

/**
 * Request envelope for `bluetooth.device.unpair` in the `bluetooth` method union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.unpair`.
 */
export interface BluetoothDeviceUnpairMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.unpair";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceUnpairRequest;
}

/**
 * Response envelope for `bluetooth.device.unpair` in the `bluetooth` method response union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.unpair`.
 */
export interface BluetoothDeviceUnpairMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.device.unpair";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDeviceUnpairResponse;
}

/**
 * Request payload for `bluetooth.device.unpair`.
 * Remove a paired device.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.unpair` request.
 */
export interface BluetoothDeviceUnpairRequest {
  /**
   * BlueZ device MAC address. Inventory field `address` emits as `address`.
   */
  address: string;
}

/**
 * Response payload for `bluetooth.device.unpair`.
 * Unpair result.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.device.unpair` response.
 */
export interface BluetoothDeviceUnpairResponse {
  /**
   * unpaired. Inventory field `status` emits as `status`.
   */
  status: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
}

/**
 * Request envelope for `bluetooth.devices.list` in the `bluetooth` method union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.devices.list`.
 */
export interface BluetoothDevicesListMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.devices.list";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDevicesListRequest;
}

/**
 * Response envelope for `bluetooth.devices.list` in the `bluetooth` method response union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.devices.list`.
 */
export interface BluetoothDevicesListMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.devices.list";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDevicesListResponse;
}

/**
 * Request payload for `bluetooth.devices.list`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.devices.list` request.
 */
export interface BluetoothDevicesListRequest {
}

/**
 * Response payload for `bluetooth.devices.list`.
 * List paired Bluetooth devices.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.devices.list` response.
 */
export interface BluetoothDevicesListResponse {
  /**
   * Paired BlueZ devices. device_info carries name/icon/class; computer-class peers are additionally annotated with device_type/connection_type macos_connector and channel 3 so the UI can echo them back on bluetooth.device.connect. Inventory field `payload` emits as `payload`.
   */
  payload: BluetoothDevicesListResponsePayload[];
  /**
   * Legacy discriminator; currently bluetooth_device_list. Inventory field `type` emits as `type`.
   */
  type: string;
}

/**
 * Opaque object shape for `payload` nested under `BluetoothDevicesListResponse`.
 * Paired BlueZ devices. device_info carries name/icon/class; computer-class peers are additionally annotated with device_type/connection_type macos_connector and channel 3 so the UI can echo them back on bluetooth.device.connect.
 * Inventory: field `payload` in `METHOD_INVENTORY` entry `bluetooth.devices.list` response.
 */
export interface BluetoothDevicesListResponsePayload {
  [key: string]: BluetoothJsonValue;
}

/**
 * Event payload for `bluetooth.discoverable`.
 * Discoverability event.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.discoverable` payload.
 */
export interface BluetoothDiscoverableEvent {
  /**
   * Requested discoverable state. Inventory field `discoverable` emits as `discoverable`.
   */
  discoverable: boolean;
}

/**
 * Event envelope for `bluetooth.discoverable` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.discoverable`.
 */
export interface BluetoothDiscoverableEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.discoverable";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDiscoverableEvent;
}

/**
 * Request envelope for `bluetooth.discoverable` in the `bluetooth` method union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.discoverable`.
 */
export interface BluetoothDiscoverableMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.discoverable";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDiscoverableRequest;
}

/**
 * Response envelope for `bluetooth.discoverable` in the `bluetooth` method response union.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.discoverable`.
 */
export interface BluetoothDiscoverableMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "bluetooth.discoverable";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothDiscoverableResponse;
}

/**
 * Request payload for `bluetooth.discoverable`.
 * Set adapter discoverability.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.discoverable` request.
 */
export interface BluetoothDiscoverableRequest {
  /**
   * Whether to request discoverable mode. Inventory field `discoverable` emits as `discoverable`.
   */
  discoverable: boolean;
}

/**
 * Response payload for `bluetooth.discoverable`.
 * Discoverability request result.
 * Inventory: `METHOD_INVENTORY` entry `bluetooth.discoverable` response.
 */
export interface BluetoothDiscoverableResponse {
  /**
   * Requested discoverable state. Inventory field `discoverable` emits as `discoverable`.
   */
  discoverable: boolean;
  /**
   * requested. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Discriminated event union for the `bluetooth` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type BluetoothEvent =
  | BluetoothAgentEventMessage
  | BluetoothPairingEventMessage
  | BluetoothConnectionEventMessage
  | BluetoothDeviceEventMessage
  | BluetoothDiscoverableEventMessage
  | BluetoothMfiEventMessage
;

/**
 * Discriminated method-request union for the `bluetooth` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type BluetoothMethod =
  | BluetoothDevicesListMethodMessage
  | BluetoothDeviceConnectMethodMessage
  | BluetoothDeviceDisconnectMethodMessage
  | BluetoothDeviceUnpairMethodMessage
  | BluetoothDiscoverableMethodMessage
;

/**
 * Discriminated method-response union for the `bluetooth` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type BluetoothMethodResponse =
  | BluetoothDevicesListMethodResponseMessage
  | BluetoothDeviceConnectMethodResponseMessage
  | BluetoothDeviceDisconnectMethodResponseMessage
  | BluetoothDeviceUnpairMethodResponseMessage
  | BluetoothDiscoverableMethodResponseMessage
;

/**
 * Event payload for `bluetooth.mfi`.
 * MFi authentication event.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.mfi` payload.
 */
export interface BluetoothMfiEvent {
  /**
   * authentication_started, authentication_succeeded, or authentication_failed. Inventory field `event` emits as `event`.
   */
  event: string;
  /**
   * Device address. Inventory field `device` emits as `device`.
   */
  device: string;
  /**
   * Failure reason. Inventory field `reason` emits as `reason`.
   */
  reason?: string;
}

/**
 * Event envelope for `bluetooth.mfi` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.mfi`.
 */
export interface BluetoothMfiEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.mfi";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothMfiEvent;
}

/**
 * Event payload for `bluetooth.pairing`.
 * Pairing event.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.pairing` payload.
 */
export interface BluetoothPairingEvent {
  /**
   * paired or unpaired. Inventory field `event` emits as `event`.
   */
  event?: string;
  /**
   * Legacy type, e.g. pairing_succeeded. Inventory field `type` emits as `type`.
   */
  type?: string;
  /**
   * Device path or address. Inventory field `device` emits as `device`.
   */
  device: string;
}

/**
 * Event envelope for `bluetooth.pairing` in the `bluetooth` event union.
 * Inventory: `EVENT_INVENTORY` entry `bluetooth.pairing`.
 */
export interface BluetoothPairingEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "bluetooth.pairing";
  /**
   * Payload carried by this inventory variant.
   */
  data: BluetoothPairingEvent;
}

