// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type MediaControlJsonValue = string | number | boolean | null | MediaControlJsonValue[] | { [key: string]: MediaControlJsonValue };

/**
 * Discriminated event union for the `media_control` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type MediaControlEvent =
  | MediaNowPlayingUpdateEventMessage
  | MediaNowPlayingArtworkEventMessage
  | MediaNowPlayingArtworkFailedEventMessage
  | PhoneVolumeUpdateEventMessage
;

/**
 * Discriminated method-request union for the `media_control` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type MediaControlMethod =
  | MediaControlPlayMethodMessage
  | MediaControlPauseMethodMessage
  | MediaControlNextMethodMessage
  | MediaControlPreviousMethodMessage
  | MediaControlShuffleMethodMessage
  | MediaControlRepeatMethodMessage
  | MediaControlVolumeUpMethodMessage
  | MediaControlVolumeDownMethodMessage
;

/**
 * Discriminated method-response union for the `media_control` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type MediaControlMethodResponse =
  | MediaControlPlayMethodResponseMessage
  | MediaControlPauseMethodResponseMessage
  | MediaControlNextMethodResponseMessage
  | MediaControlPreviousMethodResponseMessage
  | MediaControlShuffleMethodResponseMessage
  | MediaControlRepeatMethodResponseMessage
  | MediaControlVolumeUpMethodResponseMessage
  | MediaControlVolumeDownMethodResponseMessage
;

/**
 * Request envelope for `media.control.next` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.next`.
 */
export interface MediaControlNextMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.next";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlNextRequest;
}

/**
 * Response envelope for `media.control.next` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.next`.
 */
export interface MediaControlNextMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.next";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlNextResponse;
}

/**
 * Request payload for `media.control.next`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.next` request.
 */
export interface MediaControlNextRequest {
}

/**
 * Response payload for `media.control.next`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.next` response.
 */
export interface MediaControlNextResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.pause` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.pause`.
 */
export interface MediaControlPauseMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.pause";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPauseRequest;
}

/**
 * Response envelope for `media.control.pause` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.pause`.
 */
export interface MediaControlPauseMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.pause";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPauseResponse;
}

/**
 * Request payload for `media.control.pause`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.pause` request.
 */
export interface MediaControlPauseRequest {
}

/**
 * Response payload for `media.control.pause`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.pause` response.
 */
export interface MediaControlPauseResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.play` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.play`.
 */
export interface MediaControlPlayMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.play";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPlayRequest;
}

/**
 * Response envelope for `media.control.play` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.play`.
 */
export interface MediaControlPlayMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.play";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPlayResponse;
}

/**
 * Request payload for `media.control.play`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.play` request.
 */
export interface MediaControlPlayRequest {
}

/**
 * Response payload for `media.control.play`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.play` response.
 */
export interface MediaControlPlayResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.previous` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.previous`.
 */
export interface MediaControlPreviousMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.previous";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPreviousRequest;
}

/**
 * Response envelope for `media.control.previous` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.previous`.
 */
export interface MediaControlPreviousMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.previous";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlPreviousResponse;
}

/**
 * Request payload for `media.control.previous`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.previous` request.
 */
export interface MediaControlPreviousRequest {
}

/**
 * Response payload for `media.control.previous`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.previous` response.
 */
export interface MediaControlPreviousResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.repeat` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.repeat`.
 */
export interface MediaControlRepeatMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.repeat";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlRepeatRequest;
}

/**
 * Response envelope for `media.control.repeat` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.repeat`.
 */
export interface MediaControlRepeatMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.repeat";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlRepeatResponse;
}

/**
 * Request payload for `media.control.repeat`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.repeat` request.
 */
export interface MediaControlRepeatRequest {
}

/**
 * Response payload for `media.control.repeat`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.repeat` response.
 */
export interface MediaControlRepeatResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.shuffle` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.shuffle`.
 */
export interface MediaControlShuffleMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.shuffle";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlShuffleRequest;
}

/**
 * Response envelope for `media.control.shuffle` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.shuffle`.
 */
export interface MediaControlShuffleMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.shuffle";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlShuffleResponse;
}

/**
 * Request payload for `media.control.shuffle`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.shuffle` request.
 */
export interface MediaControlShuffleRequest {
}

/**
 * Response payload for `media.control.shuffle`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.shuffle` response.
 */
export interface MediaControlShuffleResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.volume_down` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_down`.
 */
export interface MediaControlVolumeDownMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.volume_down";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlVolumeDownRequest;
}

/**
 * Response envelope for `media.control.volume_down` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_down`.
 */
export interface MediaControlVolumeDownMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.volume_down";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlVolumeDownResponse;
}

/**
 * Request payload for `media.control.volume_down`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_down` request.
 */
export interface MediaControlVolumeDownRequest {
}

/**
 * Response payload for `media.control.volume_down`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_down` response.
 */
export interface MediaControlVolumeDownResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Request envelope for `media.control.volume_up` in the `media_control` method union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_up`.
 */
export interface MediaControlVolumeUpMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.volume_up";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlVolumeUpRequest;
}

/**
 * Response envelope for `media.control.volume_up` in the `media_control` method response union.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_up`.
 */
export interface MediaControlVolumeUpMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "media.control.volume_up";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaControlVolumeUpResponse;
}

/**
 * Request payload for `media.control.volume_up`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_up` request.
 */
export interface MediaControlVolumeUpRequest {
}

/**
 * Response payload for `media.control.volume_up`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `media.control.volume_up` response.
 */
export interface MediaControlVolumeUpResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Event payload for `media.now_playing.artwork`.
 * Artwork event.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.artwork` payload.
 */
export interface MediaNowPlayingArtworkEvent {
  /**
   * Base64 artwork data. Inventory field `data` emits as `data`.
   */
  data: string;
  /**
   * Artwork MIME type. Current daemon uses contentType. Inventory field `content_type` emits as `contentType`. Current source key: `contentType`.
   */
  contentType: string;
  /**
   * Optional producer generation correlating artwork with metadata. Inventory field `media_generation` emits as `mediaGeneration`.
   */
  mediaGeneration?: number;
}

/**
 * Event envelope for `media.now_playing.artwork` in the `media_control` event union.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.artwork`.
 */
export interface MediaNowPlayingArtworkEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "media.now_playing.artwork";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaNowPlayingArtworkEvent;
}

/**
 * Event payload for `media.now_playing.artwork.failed`.
 * Artwork failure event.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.artwork.failed` payload.
 */
export interface MediaNowPlayingArtworkFailedEvent {
  /**
   * Failed iAP2 file-transfer id. Inventory field `transfer_id` emits as `transferId`.
   */
  transferId: number;
}

/**
 * Event envelope for `media.now_playing.artwork.failed` in the `media_control` event union.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.artwork.failed`.
 */
export interface MediaNowPlayingArtworkFailedEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "media.now_playing.artwork.failed";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaNowPlayingArtworkFailedEvent;
}

/**
 * Event payload for `media.now_playing.update`.
 * Now-playing update. Current daemon forwards Apple-style PascalCase keys; canonical wrapper is snake_case.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.update` payload.
 */
export interface MediaNowPlayingUpdateEvent {
  /**
   * Canonical wrapper for iAP2 MediaItemAttributes. Inventory field `media_item_attributes` emits as `mediaItemAttributes`. Current source key: `MediaItemAttributes`.
   */
  mediaItemAttributes?: MediaNowPlayingUpdateEventMediaItemAttributes;
  /**
   * Canonical wrapper for iAP2 PlaybackAttributes. Inventory field `playback_attributes` emits as `playbackAttributes`. Current source key: `PlaybackAttributes`.
   */
  playbackAttributes?: MediaNowPlayingUpdateEventPlaybackAttributes;
  /**
   * Optional producer generation correlating metadata with artwork. Inventory field `media_generation` emits as `mediaGeneration`.
   */
  mediaGeneration?: number;
}

/**
 * Opaque object shape for `mediaItemAttributes` nested under `MediaNowPlayingUpdateEvent`.
 * Canonical wrapper for iAP2 MediaItemAttributes.
 * Inventory: field `media_item_attributes` in `EVENT_INVENTORY` entry `media.now_playing.update` payload.
 */
export interface MediaNowPlayingUpdateEventMediaItemAttributes {
  [key: string]: MediaControlJsonValue;
}

/**
 * Event envelope for `media.now_playing.update` in the `media_control` event union.
 * Inventory: `EVENT_INVENTORY` entry `media.now_playing.update`.
 */
export interface MediaNowPlayingUpdateEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "media.now_playing.update";
  /**
   * Payload carried by this inventory variant.
   */
  data: MediaNowPlayingUpdateEvent;
}

/**
 * Opaque object shape for `playbackAttributes` nested under `MediaNowPlayingUpdateEvent`.
 * Canonical wrapper for iAP2 PlaybackAttributes.
 * Inventory: field `playback_attributes` in `EVENT_INVENTORY` entry `media.now_playing.update` payload.
 */
export interface MediaNowPlayingUpdateEventPlaybackAttributes {
  [key: string]: MediaControlJsonValue;
}

/**
 * Event payload for `phone.volume.update`.
 * Phone volume event.
 * Inventory: `EVENT_INVENTORY` entry `phone.volume.update` payload.
 */
export interface PhoneVolumeUpdateEvent {
  /**
   * Phone media volume 0..100. Current daemon broadcasts volumePercent. Inventory field `volume_percent` emits as `volumePercent`. Current source key: `volumePercent`.
   */
  volumePercent: number;
}

/**
 * Event envelope for `phone.volume.update` in the `media_control` event union.
 * Inventory: `EVENT_INVENTORY` entry `phone.volume.update`.
 */
export interface PhoneVolumeUpdateEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "phone.volume.update";
  /**
   * Payload carried by this inventory variant.
   */
  data: PhoneVolumeUpdateEvent;
}

