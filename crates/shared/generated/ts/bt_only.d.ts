// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type BtOnlyJsonValue = string | number | boolean | null | BtOnlyJsonValue[] | { [key: string]: BtOnlyJsonValue };

/**
 * Event payload for `audio.data`.
 * Audio data frame.
 * Inventory: `EVENT_INVENTORY` entry `audio.data` payload.
 */
export interface AudioDataEvent {
  /**
   * Monotonic frame sequence. Inventory field `seq` emits as `seq`.
   */
  seq: number;
  /**
   * Base64 Opus frame. Inventory field `opus` emits as `opus`.
   */
  opus: string;
  /**
   * Unix timestamp millis. Inventory field `ts` emits as `ts`.
   */
  ts: number;
}

/**
 * Event envelope for `audio.data` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `audio.data`.
 */
export interface AudioDataEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "audio.data";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioDataEvent;
}

/**
 * Event payload for `audio.recording.started`.
 * Audio recording lifecycle start.
 * Inventory: `EVENT_INVENTORY` entry `audio.recording.started` payload.
 */
export interface AudioRecordingStartedEvent {
  /**
   * Sample rate. Current iAP2/SPP runtime uses sampleRate; msgpack tests expect sample_rate. Inventory field `sample_rate` emits as `sampleRate`. Current source key: `sampleRate`.
   */
  sampleRate: number;
  /**
   * Channel count. Inventory field `channels` emits as `channels`.
   */
  channels: number;
  /**
   * Frame duration. Current runtime uses frameMs; tests expect frame_ms. Inventory field `frame_ms` emits as `frameMs`. Current source key: `frameMs`.
   */
  frameMs: number;
}

/**
 * Event envelope for `audio.recording.started` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `audio.recording.started`.
 */
export interface AudioRecordingStartedEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "audio.recording.started";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordingStartedEvent;
}

/**
 * Event payload for `audio.recording.stopped`.
 * Audio recording lifecycle stop.
 * Inventory: `EVENT_INVENTORY` entry `audio.recording.stopped` payload.
 */
export interface AudioRecordingStoppedEvent {
  /**
   * Stop reason. Inventory field `reason` emits as `reason`.
   */
  reason: string;
  /**
   * Total frames captured. Current runtime uses totalFrames; tests expect total_frames. Inventory field `total_frames` emits as `totalFrames`. Current source key: `totalFrames`.
   */
  totalFrames: number;
}

/**
 * Event envelope for `audio.recording.stopped` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `audio.recording.stopped`.
 */
export interface AudioRecordingStoppedEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "audio.recording.stopped";
  /**
   * Payload carried by this inventory variant.
   */
  data: AudioRecordingStoppedEvent;
}

/**
 * Discriminated event union for the `bt_only` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type BtOnlyEvent =
  | DaemonReadyEventMessage
  | DaemonHeartbeatEventMessage
  | ChunkRetransmitRequestEventMessage
  | AudioRecordingStartedEventMessage
  | AudioDataEventMessage
  | AudioRecordingStoppedEventMessage
  | KeepaliveEventMessage
;

/**
 * Discriminated method-request union for the `bt_only` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type BtOnlyMethod =
  | PingMethodMessage
  | DeviceVolumeUpdateMethodMessage
;

/**
 * Discriminated method-response union for the `bt_only` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type BtOnlyMethodResponse =
  | PingMethodResponseMessage
  | DeviceVolumeUpdateMethodResponseMessage
;

/**
 * Event payload for `chunk.retransmit_request`.
 * Chunk retransmission request.
 * Inventory: `EVENT_INVENTORY` entry `chunk.retransmit_request` payload.
 */
export interface ChunkRetransmitRequestEvent {
  /**
   * UUID of the chunked message. Inventory field `message_id` emits as `messageId`.
   */
  messageId: string;
  /**
   * Zero-based chunk index to retransmit. Inventory field `chunk_idx` emits as `chunkIdx`.
   */
  chunkIdx: number;
}

/**
 * Event envelope for `chunk.retransmit_request` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `chunk.retransmit_request`.
 */
export interface ChunkRetransmitRequestEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "chunk.retransmit_request";
  /**
   * Payload carried by this inventory variant.
   */
  data: ChunkRetransmitRequestEvent;
}

/**
 * Event payload for `daemon.heartbeat`.
 * BT heartbeat event.
 * Inventory: `EVENT_INVENTORY` entry `daemon.heartbeat` payload.
 */
export interface DaemonHeartbeatEvent {
  /**
   * Unix timestamp millis. Inventory field `timestamp` emits as `timestamp`.
   */
  timestamp: number;
}

/**
 * Event envelope for `daemon.heartbeat` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `daemon.heartbeat`.
 */
export interface DaemonHeartbeatEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "daemon.heartbeat";
  /**
   * Payload carried by this inventory variant.
   */
  data: DaemonHeartbeatEvent;
}

/**
 * Event payload for `daemon.ready`.
 * No payload.
 * Inventory: `EVENT_INVENTORY` entry `daemon.ready` payload.
 */
export interface DaemonReadyEvent {
}

/**
 * Event envelope for `daemon.ready` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `daemon.ready`.
 */
export interface DaemonReadyEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "daemon.ready";
  /**
   * Payload carried by this inventory variant.
   */
  data: DaemonReadyEvent;
}

/**
 * Request envelope for `device.volume.update` in the `bt_only` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.volume.update`.
 */
export interface DeviceVolumeUpdateMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.volume.update";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceVolumeUpdateRequest;
}

/**
 * Response envelope for `device.volume.update` in the `bt_only` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.volume.update`.
 */
export interface DeviceVolumeUpdateMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.volume.update";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceVolumeUpdateResponse;
}

/**
 * Request payload for `device.volume.update`.
 * Phone volume update.
 * Inventory: `METHOD_INVENTORY` entry `device.volume.update` request.
 */
export interface DeviceVolumeUpdateRequest {
  /**
   * Phone media volume 0..100. Current app field is volumePercent. Inventory field `volume_percent` emits as `volumePercent`. Current source key: `volumePercent`.
   */
  volumePercent: number;
}

/**
 * Response payload for `device.volume.update`.
 * Volume update acknowledgement.
 * Inventory: `METHOD_INVENTORY` entry `device.volume.update` response.
 */
export interface DeviceVolumeUpdateResponse {
  /**
   * Whether update was accepted. Inventory field `success` emits as `success`.
   */
  success: boolean;
}

/**
 * Event payload for `keepalive`.
 * Companion keepalive event.
 * Inventory: `EVENT_INVENTORY` entry `keepalive` payload.
 */
export interface KeepaliveEvent {
  /**
   * Phone timestamp seconds. Inventory field `timestamp` emits as `timestamp`.
   */
  timestamp: number;
}

/**
 * Event envelope for `keepalive` in the `bt_only` event union.
 * Inventory: `EVENT_INVENTORY` entry `keepalive`.
 */
export interface KeepaliveEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "keepalive";
  /**
   * Payload carried by this inventory variant.
   */
  data: KeepaliveEvent;
}

/**
 * Request envelope for `ping` in the `bt_only` method union.
 * Inventory: `METHOD_INVENTORY` entry `ping`.
 */
export interface PingMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "ping";
  /**
   * Payload carried by this inventory variant.
   */
  data: PingRequest;
}

/**
 * Response envelope for `ping` in the `bt_only` method response union.
 * Inventory: `METHOD_INVENTORY` entry `ping`.
 */
export interface PingMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "ping";
  /**
   * Payload carried by this inventory variant.
   */
  data: PingResponse;
}

/**
 * Request payload for `ping`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `ping` request.
 */
export interface PingRequest {
}

/**
 * Response payload for `ping`.
 * Ping response.
 * Inventory: `METHOD_INVENTORY` entry `ping` response.
 */
export interface PingResponse {
  /**
   * Pong string. Inventory field `pong` emits as `pong`.
   */
  pong: string;
}

