// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen`.

export type DeviceJsonValue = string | number | boolean | null | DeviceJsonValue[] | { [key: string]: DeviceJsonValue };

/**
 * Event payload for `ambient_light_update`.
 * Ambient light reading event.
 * Inventory: `EVENT_INVENTORY` entry `ambient_light_update` payload.
 */
export interface AmbientLightUpdateEvent {
  /**
   * Raw ambient light sensor value. Inventory field `value` emits as `value`.
   */
  value: number;
  /**
   * Stock-compatible ambient darkness value from 0 to 100. Inventory field `normalized_value` emits as `normalizedValue`.
   */
  normalizedValue: number;
}

/**
 * Event envelope for `ambient_light_update` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `ambient_light_update`.
 */
export interface AmbientLightUpdateEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "ambient_light_update";
  /**
   * Payload carried by this inventory variant.
   */
  data: AmbientLightUpdateEvent;
}

/**
 * Event payload for `app.ready`.
 * Companion readiness event. Current payload mixes snake/camel; canonical is snake_case.
 * Inventory: `EVENT_INVENTORY` entry `app.ready` payload.
 */
export interface AppReadyEvent {
  /**
   * Phone datetime for daemon time sync. Inventory field `datetime` emits as `datetime`.
   */
  datetime?: string;
  /**
   * Phone timezone. Inventory field `timezone` emits as `timezone`.
   */
  timezone?: AppReadyEventTimezone;
  /**
   * ios, android, or web. Inventory field `platform` emits as `platform`.
   */
  platform?: string;
  /**
   * Subscription entitlement. Inventory field `subscribed` emits as `subscribed`.
   */
  subscribed?: boolean;
  /**
   * Subscription status; current app uses camelCase. Inventory field `subscription_status` emits as `subscriptionStatus`. Current source key: `subscriptionStatus`.
   */
  subscriptionStatus?: string;
  /**
   * Lifetime entitlement; current app uses camelCase. Inventory field `has_lifetime` emits as `hasLifetime`. Current source key: `hasLifetime`.
   */
  hasLifetime?: boolean;
  /**
   * Admin entitlement from the authenticated profile; current app uses camelCase. Inventory field `is_admin` emits as `isAdmin`. Current source key: `isAdmin`.
   */
  isAdmin?: boolean;
  /**
   * Whether entitlements were verified for the current authenticated user; current app uses camelCase. Inventory field `entitlements_verified` emits as `entitlementsVerified`. Current source key: `entitlementsVerified`.
   */
  entitlementsVerified?: boolean;
  /**
   * Spotify auth skipped; current app uses camelCase. Inventory field `spotify_skipped` emits as `spotifySkipped`. Current source key: `spotifySkipped`.
   */
  spotifySkipped?: boolean;
}

/**
 * Event envelope for `app.ready` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `app.ready`.
 */
export interface AppReadyEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "app.ready";
  /**
   * Payload carried by this inventory variant.
   */
  data: AppReadyEvent;
}

/**
 * Opaque object shape for `timezone` nested under `AppReadyEvent`.
 * Phone timezone.
 * Inventory: field `timezone` in `EVENT_INVENTORY` entry `app.ready` payload.
 */
export interface AppReadyEventTimezone {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.ab.failover` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.failover`.
 */
export interface DeviceAbFailoverMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.failover";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbFailoverRequest;
}

/**
 * Response envelope for `device.ab.failover` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.failover`.
 */
export interface DeviceAbFailoverMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.failover";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbFailoverResponse;
}

/**
 * Request payload for `device.ab.failover`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.failover` request.
 */
export interface DeviceAbFailoverRequest {
}

/**
 * Response payload for `device.ab.failover`.
 * Legacy A/B metadata JSON.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.failover` response.
 */
export interface DeviceAbFailoverResponse {
  /**
   * Active slot index. Inventory field `active_slot` emits as `activeSlot`.
   */
  activeSlot: number;
  /**
   * A or B. Inventory field `active_slot_letter` emits as `activeSlotLetter`.
   */
  activeSlotLetter: string;
  /**
   * Two slot records. Inventory field `slots` emits as `slots`.
   */
  slots: DeviceAbFailoverResponseSlots[];
  /**
   * Metadata CRC32. Inventory field `crc32` emits as `crc32`.
   */
  crc32: number;
}

/**
 * Opaque object shape for `slots` nested under `DeviceAbFailoverResponse`.
 * Two slot records.
 * Inventory: field `slots` in `METHOD_INVENTORY` entry `device.ab.failover` response.
 */
export interface DeviceAbFailoverResponseSlots {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.ab.get` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.get`.
 */
export interface DeviceAbGetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbGetRequest;
}

/**
 * Response envelope for `device.ab.get` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.get`.
 */
export interface DeviceAbGetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbGetResponse;
}

/**
 * Request payload for `device.ab.get`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.get` request.
 */
export interface DeviceAbGetRequest {
}

/**
 * Response payload for `device.ab.get`.
 * Legacy A/B metadata JSON.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.get` response.
 */
export interface DeviceAbGetResponse {
  /**
   * Active slot index. Inventory field `active_slot` emits as `activeSlot`.
   */
  activeSlot: number;
  /**
   * A or B. Inventory field `active_slot_letter` emits as `activeSlotLetter`.
   */
  activeSlotLetter: string;
  /**
   * A/B metadata major version. Inventory field `version_major` emits as `versionMajor`.
   */
  versionMajor: number;
  /**
   * A/B metadata minor version. Inventory field `version_minor` emits as `versionMinor`.
   */
  versionMinor: number;
  /**
   * Two slot records. Inventory field `slots` emits as `slots`.
   */
  slots: DeviceAbGetResponseSlots[];
  /**
   * Metadata CRC32. Inventory field `crc32` emits as `crc32`.
   */
  crc32: number;
}

/**
 * Opaque object shape for `slots` nested under `DeviceAbGetResponse`.
 * Two slot records.
 * Inventory: field `slots` in `METHOD_INVENTORY` entry `device.ab.get` response.
 */
export interface DeviceAbGetResponseSlots {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.ab.reset` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.reset`.
 */
export interface DeviceAbResetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.reset";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbResetRequest;
}

/**
 * Response envelope for `device.ab.reset` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.reset`.
 */
export interface DeviceAbResetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.reset";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbResetResponse;
}

/**
 * Request payload for `device.ab.reset`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.reset` request.
 */
export interface DeviceAbResetRequest {
}

/**
 * Response payload for `device.ab.reset`.
 * Legacy A/B metadata JSON.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.reset` response.
 */
export interface DeviceAbResetResponse {
  /**
   * Active slot index. Inventory field `active_slot` emits as `activeSlot`.
   */
  activeSlot: number;
  /**
   * A or B. Inventory field `active_slot_letter` emits as `activeSlotLetter`.
   */
  activeSlotLetter: string;
  /**
   * A/B metadata major version. Inventory field `version_major` emits as `versionMajor`.
   */
  versionMajor: number;
  /**
   * A/B metadata minor version. Inventory field `version_minor` emits as `versionMinor`.
   */
  versionMinor: number;
  /**
   * Two slot records. Inventory field `slots` emits as `slots`.
   */
  slots: DeviceAbResetResponseSlots[];
  /**
   * Metadata CRC32. Inventory field `crc32` emits as `crc32`.
   */
  crc32: number;
}

/**
 * Opaque object shape for `slots` nested under `DeviceAbResetResponse`.
 * Two slot records.
 * Inventory: field `slots` in `METHOD_INVENTORY` entry `device.ab.reset` response.
 */
export interface DeviceAbResetResponseSlots {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.ab.set_boot_result` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_boot_result`.
 */
export interface DeviceAbSetBootResultMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.set_boot_result";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbSetBootResultRequest;
}

/**
 * Response envelope for `device.ab.set_boot_result` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_boot_result`.
 */
export interface DeviceAbSetBootResultMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.set_boot_result";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbSetBootResultResponse;
}

/**
 * Request payload for `device.ab.set_boot_result`.
 * Set boot result.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_boot_result` request.
 */
export interface DeviceAbSetBootResultRequest {
  /**
   * 0 triggers failover; 1 marks active boot successful. Inventory field `result` emits as `result`.
   */
  result: number;
}

/**
 * Response payload for `device.ab.set_boot_result`.
 * Legacy A/B metadata JSON.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_boot_result` response.
 */
export interface DeviceAbSetBootResultResponse {
  /**
   * Active slot index. Inventory field `active_slot` emits as `activeSlot`.
   */
  activeSlot: number;
  /**
   * A or B. Inventory field `active_slot_letter` emits as `activeSlotLetter`.
   */
  activeSlotLetter: string;
  /**
   * Two slot records. Inventory field `slots` emits as `slots`.
   */
  slots: DeviceAbSetBootResultResponseSlots[];
  /**
   * Metadata CRC32. Inventory field `crc32` emits as `crc32`.
   */
  crc32: number;
}

/**
 * Opaque object shape for `slots` nested under `DeviceAbSetBootResultResponse`.
 * Two slot records.
 * Inventory: field `slots` in `METHOD_INVENTORY` entry `device.ab.set_boot_result` response.
 */
export interface DeviceAbSetBootResultResponseSlots {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.ab.set_slot` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_slot`.
 */
export interface DeviceAbSetSlotMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.set_slot";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbSetSlotRequest;
}

/**
 * Response envelope for `device.ab.set_slot` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_slot`.
 */
export interface DeviceAbSetSlotMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.ab.set_slot";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceAbSetSlotResponse;
}

/**
 * Request payload for `device.ab.set_slot`.
 * Select active slot.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_slot` request.
 */
export interface DeviceAbSetSlotRequest {
  /**
   * Slot index, 0 or 1. Inventory field `slot` emits as `slot`.
   */
  slot: number;
}

/**
 * Response payload for `device.ab.set_slot`.
 * Legacy A/B metadata JSON.
 * Inventory: `METHOD_INVENTORY` entry `device.ab.set_slot` response.
 */
export interface DeviceAbSetSlotResponse {
  /**
   * Active slot index. Inventory field `active_slot` emits as `activeSlot`.
   */
  activeSlot: number;
  /**
   * A or B. Inventory field `active_slot_letter` emits as `activeSlotLetter`.
   */
  activeSlotLetter: string;
  /**
   * A/B metadata major version. Inventory field `version_major` emits as `versionMajor`.
   */
  versionMajor: number;
  /**
   * A/B metadata minor version. Inventory field `version_minor` emits as `versionMinor`.
   */
  versionMinor: number;
  /**
   * Two slot records. Inventory field `slots` emits as `slots`.
   */
  slots: DeviceAbSetSlotResponseSlots[];
  /**
   * Metadata CRC32. Inventory field `crc32` emits as `crc32`.
   */
  crc32: number;
}

/**
 * Opaque object shape for `slots` nested under `DeviceAbSetSlotResponse`.
 * Two slot records.
 * Inventory: field `slots` in `METHOD_INVENTORY` entry `device.ab.set_slot` response.
 */
export interface DeviceAbSetSlotResponseSlots {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.brightness.auto` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.auto`.
 */
export interface DeviceBrightnessAutoMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.auto";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessAutoRequest;
}

/**
 * Response envelope for `device.brightness.auto` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.auto`.
 */
export interface DeviceBrightnessAutoMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.auto";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessAutoResponse;
}

/**
 * Request payload for `device.brightness.auto`.
 * Enable or disable native auto brightness.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.auto` request.
 */
export interface DeviceBrightnessAutoRequest {
  /**
   * Whether native auto-brightness should run. Inventory field `enabled` emits as `enabled`.
   */
  enabled: boolean;
}

/**
 * Response payload for `device.brightness.auto`.
 * Brightness configuration response.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.auto` response.
 */
export interface DeviceBrightnessAutoResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
}

/**
 * Request envelope for `device.brightness.get` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.get`.
 */
export interface DeviceBrightnessGetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessGetRequest;
}

/**
 * Response envelope for `device.brightness.get` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.get`.
 */
export interface DeviceBrightnessGetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessGetResponse;
}

/**
 * Request payload for `device.brightness.get`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.get` request.
 */
export interface DeviceBrightnessGetRequest {
}

/**
 * Response payload for `device.brightness.get`.
 * Brightness configuration response.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.get` response.
 */
export interface DeviceBrightnessGetResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
}

/**
 * Request envelope for `device.brightness.set` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.set`.
 */
export interface DeviceBrightnessSetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.set";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessSetRequest;
}

/**
 * Response envelope for `device.brightness.set` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.set`.
 */
export interface DeviceBrightnessSetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.brightness.set";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceBrightnessSetResponse;
}

/**
 * Request payload for `device.brightness.set`.
 * Set manual brightness.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.set` request.
 */
export interface DeviceBrightnessSetRequest {
  /**
   * Backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
}

/**
 * Response payload for `device.brightness.set`.
 * Brightness configuration response.
 * Inventory: `METHOD_INVENTORY` entry `device.brightness.set` response.
 */
export interface DeviceBrightnessSetResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
}

/**
 * Request envelope for `device.display.get` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.get`.
 */
export interface DeviceDisplayGetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplayGetRequest;
}

/**
 * Response envelope for `device.display.get` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.get`.
 */
export interface DeviceDisplayGetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplayGetResponse;
}

/**
 * Request payload for `device.display.get`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.display.get` request.
 */
export interface DeviceDisplayGetRequest {
}

/**
 * Response payload for `device.display.get`.
 * Display sleep state response.
 * Inventory: `METHOD_INVENTORY` entry `device.display.get` response.
 */
export interface DeviceDisplayGetResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Saved backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
  /**
   * Whether the display backlight is sleeping. Inventory field `sleeping` emits as `sleeping`.
   */
  sleeping: boolean;
}

/**
 * Request envelope for `device.display.sleep` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.sleep`.
 */
export interface DeviceDisplaySleepMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.sleep";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplaySleepRequest;
}

/**
 * Response envelope for `device.display.sleep` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.sleep`.
 */
export interface DeviceDisplaySleepMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.sleep";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplaySleepResponse;
}

/**
 * Request payload for `device.display.sleep`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.display.sleep` request.
 */
export interface DeviceDisplaySleepRequest {
}

/**
 * Response payload for `device.display.sleep`.
 * Display sleep state response.
 * Inventory: `METHOD_INVENTORY` entry `device.display.sleep` response.
 */
export interface DeviceDisplaySleepResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Saved backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
  /**
   * Whether the display backlight is sleeping. Inventory field `sleeping` emits as `sleeping`.
   */
  sleeping: boolean;
}

/**
 * Request envelope for `device.display.wake` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.wake`.
 */
export interface DeviceDisplayWakeMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.wake";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplayWakeRequest;
}

/**
 * Response envelope for `device.display.wake` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.display.wake`.
 */
export interface DeviceDisplayWakeMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.display.wake";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceDisplayWakeResponse;
}

/**
 * Request payload for `device.display.wake`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.display.wake` request.
 */
export interface DeviceDisplayWakeRequest {
}

/**
 * Response payload for `device.display.wake`.
 * Display sleep state response.
 * Inventory: `METHOD_INVENTORY` entry `device.display.wake` response.
 */
export interface DeviceDisplayWakeResponse {
  /**
   * Whether automatic brightness is enabled. Inventory field `auto` emits as `auto`.
   */
  auto: boolean;
  /**
   * Saved backlight value, inverted Car Thing scale 0..160. Inventory field `brightness` emits as `brightness`.
   */
  brightness: number;
  /**
   * Whether the display backlight is sleeping. Inventory field `sleeping` emits as `sleeping`.
   */
  sleeping: boolean;
}

/**
 * Discriminated event union for the `device` inventory family.
 * Inventory: `EVENT_INVENTORY` entries grouped by `Family`.
 */
export type DeviceEvent =
  | AppReadyEventMessage
  | SubscriptionUpdatedEventMessage
  | NetworkStatusEventMessage
  | NotificationShowEventMessage
  | NotificationRemoveEventMessage
  | AmbientLightUpdateEventMessage
;

/**
 * Request envelope for `device.factory_reset` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.factory_reset`.
 */
export interface DeviceFactoryResetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.factory_reset";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceFactoryResetRequest;
}

/**
 * Response envelope for `device.factory_reset` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.factory_reset`.
 */
export interface DeviceFactoryResetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.factory_reset";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceFactoryResetResponse;
}

/**
 * Request payload for `device.factory_reset`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.factory_reset` request.
 */
export interface DeviceFactoryResetRequest {
}

/**
 * Response payload for `device.factory_reset`.
 * Boolean success response used by daemon-admin methods.
 * Inventory: `METHOD_INVENTORY` entry `device.factory_reset` response.
 */
export interface DeviceFactoryResetResponse {
  /**
   * Whether the command succeeded. Inventory field `success` emits as `success`.
   */
  success: boolean;
  /**
   * Error text when success is false. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Request envelope for `device.info` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.info`.
 */
export interface DeviceInfoMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.info";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceInfoRequest;
}

/**
 * Response envelope for `device.info` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.info`.
 */
export interface DeviceInfoMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.info";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceInfoResponse;
}

/**
 * Request payload for `device.info`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.info` request.
 */
export interface DeviceInfoRequest {
}

/**
 * Response payload for `device.info`.
 * Device metadata. Current daemon serializes this struct as camelCase; inventory canonicalizes fields.
 * Inventory: `METHOD_INVENTORY` entry `device.info` response.
 */
export interface DeviceInfoResponse {
  /**
   * Bluetooth display name. Inventory field `device` emits as `device`.
   */
  device: string;
  /**
   * Short firmware version. Inventory field `version` emits as `version`.
   */
  version: string;
  /**
   * Full firmware version. Inventory field `full_version` emits as `fullVersion`. Current source key: `fullVersion`.
   */
  fullVersion?: string;
  /**
   * Exact version baked into the running rootfs image. Inventory field `image_version` emits as `imageVersion`. Current source key: `imageVersion`.
   */
  imageVersion?: string;
  /**
   * Version of the active daemon and webapp overlay, falling back to the rootfs image version. Inventory field `bandaid_version` emits as `bandaidVersion`. Current source key: `bandaidVersion`.
   */
  bandaidVersion?: string;
  /**
   * Build date. Inventory field `build_date` emits as `buildDate`. Current source key: `buildDate`.
   */
  buildDate?: string;
  /**
   * Build git hash. Inventory field `git_hash` emits as `gitHash`. Current source key: `gitHash`.
   */
  gitHash?: string;
  /**
   * Car Thing serial number. Inventory field `serial_number` emits as `serialNumber`. Current source key: `serialNumber`.
   */
  serialNumber?: string;
}

/**
 * Request envelope for `device.launch_app` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.launch_app`.
 */
export interface DeviceLaunchAppMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.launch_app";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceLaunchAppRequest;
}

/**
 * Response envelope for `device.launch_app` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.launch_app`.
 */
export interface DeviceLaunchAppMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.launch_app";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceLaunchAppResponse;
}

/**
 * Request payload for `device.launch_app`.
 * Request the phone to launch an app. Current WS field is bundleId.
 * Inventory: `METHOD_INVENTORY` entry `device.launch_app` request.
 */
export interface DeviceLaunchAppRequest {
  /**
   * iOS app bundle identifier; defaults to com.usenocturne.nocturne. Inventory field `bundle_id` emits as `bundleId`. Current source key: `bundleId`.
   */
  bundleId?: string;
}

/**
 * Response payload for `device.launch_app`.
 * Simple status response.
 * Inventory: `METHOD_INVENTORY` entry `device.launch_app` response.
 */
export interface DeviceLaunchAppResponse {
  /**
   * Operation status string. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Discriminated method-request union for the `device` inventory family.
 * Inventory: `METHOD_INVENTORY` entries grouped by `Family`.
 */
export type DeviceMethod =
  | DeviceVersionMethodMessage
  | DeviceInfoMethodMessage
  | DeviceLaunchAppMethodMessage
  | DeviceTimezoneGetMethodMessage
  | DeviceTimeGetMethodMessage
  | DevicePowerRebootMethodMessage
  | DevicePowerShutdownMethodMessage
  | DevicePowerOffMethodMessage
  | DeviceFactoryResetMethodMessage
  | ResetBootCounterMethodMessage
  | DeviceBrightnessGetMethodMessage
  | DeviceBrightnessSetMethodMessage
  | DeviceBrightnessAutoMethodMessage
  | DeviceDisplayGetMethodMessage
  | DeviceDisplaySleepMethodMessage
  | DeviceDisplayWakeMethodMessage
  | DeviceAbGetMethodMessage
  | DeviceAbResetMethodMessage
  | DeviceAbSetSlotMethodMessage
  | DeviceAbSetBootResultMethodMessage
  | DeviceAbFailoverMethodMessage
  | OnboardingSetStateMethodMessage
;

/**
 * Discriminated method-response union for the `device` inventory family.
 * Inventory: `METHOD_INVENTORY` response entries grouped by `Family`.
 */
export type DeviceMethodResponse =
  | DeviceVersionMethodResponseMessage
  | DeviceInfoMethodResponseMessage
  | DeviceLaunchAppMethodResponseMessage
  | DeviceTimezoneGetMethodResponseMessage
  | DeviceTimeGetMethodResponseMessage
  | DevicePowerRebootMethodResponseMessage
  | DevicePowerShutdownMethodResponseMessage
  | DevicePowerOffMethodResponseMessage
  | DeviceFactoryResetMethodResponseMessage
  | ResetBootCounterMethodResponseMessage
  | DeviceBrightnessGetMethodResponseMessage
  | DeviceBrightnessSetMethodResponseMessage
  | DeviceBrightnessAutoMethodResponseMessage
  | DeviceDisplayGetMethodResponseMessage
  | DeviceDisplaySleepMethodResponseMessage
  | DeviceDisplayWakeMethodResponseMessage
  | DeviceAbGetMethodResponseMessage
  | DeviceAbResetMethodResponseMessage
  | DeviceAbSetSlotMethodResponseMessage
  | DeviceAbSetBootResultMethodResponseMessage
  | DeviceAbFailoverMethodResponseMessage
  | OnboardingSetStateMethodResponseMessage
;

/**
 * Request envelope for `device.power.off` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.off`.
 */
export interface DevicePowerOffMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.off";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerOffRequest;
}

/**
 * Response envelope for `device.power.off` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.off`.
 */
export interface DevicePowerOffMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.off";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerOffResponse;
}

/**
 * Request payload for `device.power.off`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.power.off` request.
 */
export interface DevicePowerOffRequest {
}

/**
 * Response payload for `device.power.off`.
 * Boolean success response used by daemon-admin methods.
 * Inventory: `METHOD_INVENTORY` entry `device.power.off` response.
 */
export interface DevicePowerOffResponse {
  /**
   * Whether the command succeeded. Inventory field `success` emits as `success`.
   */
  success: boolean;
  /**
   * Error text when success is false. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Request envelope for `device.power.reboot` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.reboot`.
 */
export interface DevicePowerRebootMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.reboot";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerRebootRequest;
}

/**
 * Response envelope for `device.power.reboot` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.reboot`.
 */
export interface DevicePowerRebootMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.reboot";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerRebootResponse;
}

/**
 * Request payload for `device.power.reboot`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.power.reboot` request.
 */
export interface DevicePowerRebootRequest {
}

/**
 * Response payload for `device.power.reboot`.
 * Boolean success response used by daemon-admin methods.
 * Inventory: `METHOD_INVENTORY` entry `device.power.reboot` response.
 */
export interface DevicePowerRebootResponse {
  /**
   * Whether the command succeeded. Inventory field `success` emits as `success`.
   */
  success: boolean;
  /**
   * Error text when success is false. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Request envelope for `device.power.shutdown` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.shutdown`.
 */
export interface DevicePowerShutdownMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.shutdown";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerShutdownRequest;
}

/**
 * Response envelope for `device.power.shutdown` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.power.shutdown`.
 */
export interface DevicePowerShutdownMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.power.shutdown";
  /**
   * Payload carried by this inventory variant.
   */
  data: DevicePowerShutdownResponse;
}

/**
 * Request payload for `device.power.shutdown`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.power.shutdown` request.
 */
export interface DevicePowerShutdownRequest {
}

/**
 * Response payload for `device.power.shutdown`.
 * Boolean success response used by daemon-admin methods.
 * Inventory: `METHOD_INVENTORY` entry `device.power.shutdown` response.
 */
export interface DevicePowerShutdownResponse {
  /**
   * Whether the command succeeded. Inventory field `success` emits as `success`.
   */
  success: boolean;
  /**
   * Error text when success is false. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Request envelope for `device.time.get` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.time.get`.
 */
export interface DeviceTimeGetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.time.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceTimeGetRequest;
}

/**
 * Response envelope for `device.time.get` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.time.get`.
 */
export interface DeviceTimeGetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.time.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceTimeGetResponse;
}

/**
 * Request payload for `device.time.get`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.time.get` request.
 */
export interface DeviceTimeGetRequest {
}

/**
 * Response payload for `device.time.get`.
 * Phone time response.
 * Inventory: `METHOD_INVENTORY` entry `device.time.get` response.
 */
export interface DeviceTimeGetResponse {
  /**
   * Phone-provided date string accepted by date -s. Inventory field `datetime` emits as `datetime`.
   */
  datetime: string;
  /**
   * Phone-provided local HH:mm:ss time string for UI display. Inventory field `time` emits as `time`.
   */
  time?: string;
}

/**
 * Request envelope for `device.timezone.get` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.timezone.get`.
 */
export interface DeviceTimezoneGetMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.timezone.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceTimezoneGetRequest;
}

/**
 * Response envelope for `device.timezone.get` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.timezone.get`.
 */
export interface DeviceTimezoneGetMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.timezone.get";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceTimezoneGetResponse;
}

/**
 * Request payload for `device.timezone.get`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.timezone.get` request.
 */
export interface DeviceTimezoneGetRequest {
}

/**
 * Response payload for `device.timezone.get`.
 * Phone timezone response.
 * Inventory: `METHOD_INVENTORY` entry `device.timezone.get` response.
 */
export interface DeviceTimezoneGetResponse {
  /**
   * Phone timezone object. Inventory field `timezone` emits as `timezone`.
   */
  timezone: DeviceTimezoneGetResponseTimezone;
}

/**
 * Opaque object shape for `timezone` nested under `DeviceTimezoneGetResponse`.
 * Phone timezone object.
 * Inventory: field `timezone` in `METHOD_INVENTORY` entry `device.timezone.get` response.
 */
export interface DeviceTimezoneGetResponseTimezone {
  [key: string]: DeviceJsonValue;
}

/**
 * Request envelope for `device.version` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `device.version`.
 */
export interface DeviceVersionMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.version";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceVersionRequest;
}

/**
 * Response envelope for `device.version` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `device.version`.
 */
export interface DeviceVersionMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "device.version";
  /**
   * Payload carried by this inventory variant.
   */
  data: DeviceVersionResponse;
}

/**
 * Request payload for `device.version`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `device.version` request.
 */
export interface DeviceVersionRequest {
}

/**
 * Response payload for `device.version`.
 * Version metadata derived from /etc/superbird. Inventory canonicalizes camelCase wire fields to snake_case.
 * Inventory: `METHOD_INVENTORY` entry `device.version` response.
 */
export interface DeviceVersionResponse {
  /**
   * Full firmware version. Inventory field `version` emits as `version`.
   */
  version?: string;
  /**
   * Short firmware version. Inventory field `short_version` emits as `shortVersion`. Current source key: `shortVersion`.
   */
  shortVersion?: string;
  /**
   * Exact version baked into the running rootfs image. Inventory field `image_version` emits as `imageVersion`. Current source key: `imageVersion`.
   */
  imageVersion?: string;
  /**
   * Version of the active daemon and webapp overlay, falling back to the rootfs image version. Inventory field `bandaid_version` emits as `bandaidVersion`. Current source key: `bandaidVersion`.
   */
  bandaidVersion?: string;
  /**
   * Build git hash. Inventory field `git_hash` emits as `gitHash`. Current source key: `gitHash`.
   */
  gitHash?: string;
  /**
   * Build date. Inventory field `build_date` emits as `buildDate`. Current source key: `buildDate`.
   */
  buildDate?: string;
  /**
   * Read/parse error. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Event payload for `network.status`.
 * Network connectivity event.
 * Inventory: `EVENT_INVENTORY` entry `network.status` payload.
 */
export interface NetworkStatusEvent {
  /**
   * connected or disconnected. Inventory field `status` emits as `status`.
   */
  status: string;
}

/**
 * Event envelope for `network.status` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `network.status`.
 */
export interface NetworkStatusEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "network.status";
  /**
   * Payload carried by this inventory variant.
   */
  data: NetworkStatusEvent;
}

/**
 * Event payload for `notification.remove`.
 * Notification removal event.
 * Inventory: `EVENT_INVENTORY` entry `notification.remove` payload.
 */
export interface NotificationRemoveEvent {
  /**
   * Notification id to remove. Inventory field `id` emits as `id`.
   */
  id: string;
}

/**
 * Event envelope for `notification.remove` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `notification.remove`.
 */
export interface NotificationRemoveEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "notification.remove";
  /**
   * Payload carried by this inventory variant.
   */
  data: NotificationRemoveEvent;
}

/**
 * Event payload for `notification.show`.
 * Notification event.
 * Inventory: `EVENT_INVENTORY` entry `notification.show` payload.
 */
export interface NotificationShowEvent {
  /**
   * Notification id. Inventory field `id` emits as `id`.
   */
  id?: string;
  /**
   * Notification title. Inventory field `title` emits as `title`.
   */
  title: string;
  /**
   * Notification body. Inventory field `body` emits as `body`.
   */
  body?: string;
  /**
   * Secondary notification text. Inventory field `subtitle` emits as `subtitle`.
   */
  subtitle?: string;
  /**
   * Notification category. Inventory field `category` emits as `category`.
   */
  category?: string;
  /**
   * Subscription expiry countdown; current app uses camelCase. Inventory field `days_until_expiry` emits as `daysUntilExpiry`. Current source key: `daysUntilExpiry`.
   */
  daysUntilExpiry?: number;
  /**
   * Unix timestamp millis. Inventory field `timestamp` emits as `timestamp`.
   */
  timestamp?: number;
  /**
   * Originating iOS app bundle identifier. Inventory field `app_bundle_id` emits as `appBundleId`.
   */
  appBundleId?: string;
  /**
   * Originating app display name when available. Inventory field `app_name` emits as `appName`.
   */
  appName?: string;
  /**
   * Whether the source notification is silent. Inventory field `silent` emits as `silent`.
   */
  silent?: boolean;
  /**
   * Whether the source marked the notification important. Inventory field `important` emits as `important`.
   */
  important?: boolean;
  /**
   * Whether the notification predates the current ANCS session. Inventory field `pre_existing` emits as `preExisting`.
   */
  preExisting?: boolean;
}

/**
 * Event envelope for `notification.show` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `notification.show`.
 */
export interface NotificationShowEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "notification.show";
  /**
   * Payload carried by this inventory variant.
   */
  data: NotificationShowEvent;
}

/**
 * Request envelope for `onboarding.set_state` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `onboarding.set_state`.
 */
export interface OnboardingSetStateMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "onboarding.set_state";
  /**
   * Payload carried by this inventory variant.
   */
  data: OnboardingSetStateRequest;
}

/**
 * Response envelope for `onboarding.set_state` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `onboarding.set_state`.
 */
export interface OnboardingSetStateMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "onboarding.set_state";
  /**
   * Payload carried by this inventory variant.
   */
  data: OnboardingSetStateResponse;
}

/**
 * Request payload for `onboarding.set_state`.
 * Onboarding state update.
 * Inventory: `METHOD_INVENTORY` entry `onboarding.set_state` request.
 */
export interface OnboardingSetStateRequest {
  /**
   * Onboarding state selected by the UI/app. Inventory field `state` emits as `state`.
   */
  state: string;
}

/**
 * Response payload for `onboarding.set_state`.
 * Opaque JSON payload owned by the companion app or legacy daemon code.
 * Inventory: `METHOD_INVENTORY` entry `onboarding.set_state` response.
 */
export interface OnboardingSetStateResponse {
}

/**
 * Request envelope for `reset_boot_counter` in the `device` method union.
 * Inventory: `METHOD_INVENTORY` entry `reset_boot_counter`.
 */
export interface ResetBootCounterMethodMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "reset_boot_counter";
  /**
   * Payload carried by this inventory variant.
   */
  data: ResetBootCounterRequest;
}

/**
 * Response envelope for `reset_boot_counter` in the `device` method response union.
 * Inventory: `METHOD_INVENTORY` entry `reset_boot_counter`.
 */
export interface ResetBootCounterMethodResponseMessage {
  /**
   * Discriminator from the inventory `method` tag.
   */
  method: "reset_boot_counter";
  /**
   * Payload carried by this inventory variant.
   */
  data: ResetBootCounterResponse;
}

/**
 * Request payload for `reset_boot_counter`.
 * No payload.
 * Inventory: `METHOD_INVENTORY` entry `reset_boot_counter` request.
 */
export interface ResetBootCounterRequest {
}

/**
 * Response payload for `reset_boot_counter`.
 * Boolean success response used by daemon-admin methods.
 * Inventory: `METHOD_INVENTORY` entry `reset_boot_counter` response.
 */
export interface ResetBootCounterResponse {
  /**
   * Whether the command succeeded. Inventory field `success` emits as `success`.
   */
  success: boolean;
  /**
   * Error text when success is false. Inventory field `error` emits as `error`.
   */
  error?: string;
}

/**
 * Event payload for `subscription.updated`.
 * Subscription update event.
 * Inventory: `EVENT_INVENTORY` entry `subscription.updated` payload.
 */
export interface SubscriptionUpdatedEvent {
  /**
   * Subscription entitlement. Inventory field `subscribed` emits as `subscribed`.
   */
  subscribed?: boolean;
  /**
   * Subscription status; current app uses camelCase. Inventory field `subscription_status` emits as `subscriptionStatus`. Current source key: `subscriptionStatus`.
   */
  subscriptionStatus?: string;
  /**
   * Lifetime entitlement; current app uses camelCase. Inventory field `has_lifetime` emits as `hasLifetime`. Current source key: `hasLifetime`.
   */
  hasLifetime?: boolean;
  /**
   * Admin entitlement from the authenticated profile; current app uses camelCase. Inventory field `is_admin` emits as `isAdmin`. Current source key: `isAdmin`.
   */
  isAdmin?: boolean;
  /**
   * Whether entitlements were verified for the current authenticated user; current app uses camelCase. Inventory field `entitlements_verified` emits as `entitlementsVerified`. Current source key: `entitlementsVerified`.
   */
  entitlementsVerified?: boolean;
}

/**
 * Event envelope for `subscription.updated` in the `device` event union.
 * Inventory: `EVENT_INVENTORY` entry `subscription.updated`.
 */
export interface SubscriptionUpdatedEventMessage {
  /**
   * Discriminator from the inventory `event` tag.
   */
  event: "subscription.updated";
  /**
   * Payload carried by this inventory variant.
   */
  data: SubscriptionUpdatedEvent;
}

