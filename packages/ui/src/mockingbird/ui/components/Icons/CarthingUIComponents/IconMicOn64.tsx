import type { FixedIconProps } from "../IconProps";
import { IconMic } from "../EncoreWeb/IconMic";

const IconMicOn64 = (props: FixedIconProps) => (
  <IconMic {...props} iconSize={64} />
);

export default IconMicOn64;
