import type { FixedIconProps } from "../IconProps";
import { IconHomeActive } from "../EncoreWeb/IconHomeActive";

const IconHomeActive32 = (props: FixedIconProps) => (
  <IconHomeActive {...props} iconSize={32} />
);

export default IconHomeActive32;
