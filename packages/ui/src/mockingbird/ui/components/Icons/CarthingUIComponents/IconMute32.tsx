import type { FixedIconProps } from "../IconProps";
import { IconMicOff } from "../EncoreWeb/IconMicOff";

const IconMute32 = (props: FixedIconProps) => (
  <IconMicOff {...props} iconSize={32} />
);

export default IconMute32;
