import type { FixedIconProps } from "../IconProps";
import { IconCheckAlt } from "../EncoreWeb/IconCheckAlt";

const IconCheckAlt48 = (props: FixedIconProps) => (
  <IconCheckAlt {...props} iconSize={48} />
);

export default IconCheckAlt48;
