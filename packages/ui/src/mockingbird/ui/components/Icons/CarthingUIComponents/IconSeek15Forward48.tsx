import type { FixedIconProps } from "../IconProps";
import { IconSkipForward15 } from "../EncoreWeb/IconSkipForward15";

const IconSeek15Forward48 = (props: FixedIconProps) => (
  <IconSkipForward15 {...props} iconSize={48} />
);

export default IconSeek15Forward48;
