import type { MockIconProps } from "../IconProps";
import IconRepeat from "./IconRepeat";

const IconRepeat32 = (props: MockIconProps) => (
  <IconRepeat {...props} iconSize={32} />
);

export default IconRepeat32;
