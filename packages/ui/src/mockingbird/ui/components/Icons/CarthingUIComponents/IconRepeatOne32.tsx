import type { MockIconProps } from "../IconProps";
import IconRepeatOne from "./IconRepeatOne";

const IconRepeatOne32 = (props: MockIconProps) => (
  <IconRepeatOne {...props} iconSize={32} />
);

export default IconRepeatOne32;
