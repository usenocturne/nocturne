import type { FixedIconProps } from "../IconProps";
import { IconPlay } from "../EncoreWeb/IconPlay";

const IconPlay48 = (props: FixedIconProps) => (
  <IconPlay {...props} iconSize={48} />
);

export default IconPlay48;
