import type { FixedIconProps } from "../IconProps";
import { IconCollection } from "../EncoreWeb/IconCollection";

const IconLibrary32 = (props: FixedIconProps) => (
  <IconCollection {...props} iconSize={32} />
);

export default IconLibrary32;
