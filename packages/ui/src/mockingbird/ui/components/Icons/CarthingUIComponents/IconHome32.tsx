import type { FixedIconProps } from "../IconProps";
import { IconHome } from "../EncoreWeb/IconHome";

const IconHome32 = (props: FixedIconProps) => (
  <IconHome {...props} iconSize={32} />
);

export default IconHome32;
