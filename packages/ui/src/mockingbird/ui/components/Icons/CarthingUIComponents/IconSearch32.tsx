import type { FixedIconProps } from "../IconProps";
import { IconSearch } from "../EncoreWeb/IconSearch";

const IconSearch32 = (props: FixedIconProps) => (
  <IconSearch {...props} iconSize={32} />
);

export default IconSearch32;
